import org.junit.jupiter.api.Test
import kotlin.test.assertEquals

class SampleTest {
    @Test
    fun add() {
        val readInput = readInput(1)
        readInput.forEach { println(it) }
        assertEquals("ab", "ab")
    }
}